<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Autenticação - Instagram</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'authentication' %>
    <%= javascript_pack_tag 'authentication' %>
  </head>

  <body data-controller="flash-notifications" data-flash-messages="<%= flash.to_hash.to_json %>">
    <main>
      <section class="hero">
        <div class="logo">
          <%= image_tag "instagram.png" %>
        </div>

        <%= yield %>
      </section>
    </main>
  </body>
</html>
