<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body data-controller="flash-notifications" data-flash-messages="<%= flash.to_json %>">
    <%= turbo_stream_from 'flash_notifications' %>
    <div id="flash-notifications"></div>

    <header>
      <div class="container">
        <nav>
          <div class="logo-icon">
            <%= link_to embedded_svg("instagram-icon.svg"), root_path %>
          </div>

          <div class="logo-text">
            <%= link_to root_path do %>
              <%= image_tag "instagram.png" %>
            <% end %>
          </div>
        </nav>

        <nav class="actions">
          <%= link_to embedded_svg("plus.svg"), new_post_path, data: { turbo_frame: dom_id(Post.new) } %>
          <%= turbo_frame_tag Post.new %>
          
          <%= link_to embedded_svg("user.svg"), "#", data: { controller: "modal", action: "click->modal#open", modal_id: "user-actions" } %>
        </nav>
      </div>
    </header>

    <%= yield %>

    <%= render "layouts/user_actions_modal" %>
  </body>
</html>
